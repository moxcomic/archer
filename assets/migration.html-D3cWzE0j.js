import{_ as o,c as t,a as c,o as a}from"./app-ByQJVS6B.js";const n={};function i(s,e){return a(),t("div",null,[...e[0]||(e[0]=[c(`<h1 id="script-package-path-migration" tabindex="-1"><a class="header-anchor" href="#script-package-path-migration"><span>Script Package Path Migration</span></a></h1><blockquote><p><strong>Who this is for</strong>: developers who push <strong>remote yaegi scripts</strong> through the <code>/auto</code> endpoint, and anyone maintaining local <code>scripts/*.majsoul</code> samples.</p><p>This refactor moved the script-visible Go packages into the layered <code>internal/</code> directory. <strong>The import paths in your scripts need updating</strong>, otherwise yaegi will fail at load/runtime with <code>unable to find source</code> / <code>undefined symbol</code>.</p></blockquote><blockquote><p>⚠️ <strong>2026 update (Majsoul Unity migration)</strong>: After Majsoul&#39;s web client moved to Unity + WASM, all the UI-state “manager” packages have been <strong>removed entirely</strong>: <code>lobbymgr</code>, <code>desktopmgr</code>, <code>infomgr</code>, <code>gameendmgr</code>, <code>activitymgr</code>, <code>activitysignmgr</code>, <code>monthlymgr</code>, <code>countdown</code>, <code>playerinfo</code>. They relied on LayaBox engine JS globals that no longer exist under Unity. <strong>The widget rows in table A below, and sections B2 / B3, are obsolete — do not use them</strong>; derive game state from the in-game protobuf event stream instead. The other packages (<code>gamestate</code> / <code>userinfo</code> / <code>variable</code> / <code>actions</code> / <code>pusher</code>, etc.) and the <code>utils</code> split (B1) are unaffected.</p></blockquote><h2 id="tl-dr" tabindex="-1"><a class="header-anchor" href="#tl-dr"><span>TL;DR</span></a></h2><ul><li><strong>For the vast majority of packages: only the import path changes.</strong> The <code>pkg.Inst().Method()</code> call style is <strong>completely unchanged</strong>.</li><li><strong><code>utils</code> was split into 4 packages</strong> (drop <code>.Inst()</code>, call package-level functions instead — see B1). (The merged <code>countdown</code> / <code>playerinfo</code> UI-state packages have since been removed in the Unity migration — see the notice above.)</li><li><strong>Method names never change.</strong> The <code>config</code> path is unchanged. The engine packages <code>github.com/moxcomic/lq</code> and <code>github.com/moxcomic/engine/tenhou_client</code> are unaffected.</li></ul><h2 id="why-it-changed" tabindex="-1"><a class="header-anchor" href="#why-it-changed"><span>Why it changed</span></a></h2><p>The yaegi interpreter uses a symbols table to map the import paths in your script to the real Go packages. After the refactor these packages physically moved (new import paths), and the symbols table was updated to match. But <strong>the import paths hard-coded in your scripts must be changed by you</strong> — if a path doesn&#39;t match, yaegi can&#39;t resolve the symbol and errors out at load or runtime.</p><h2 id="a-import-path-only-call-style-unchanged" tabindex="-1"><a class="header-anchor" href="#a-import-path-only-call-style-unchanged"><span>A. Import path only (call style unchanged)</span></a></h2><table><thead><tr><th>Old import</th><th>New import</th></tr></thead><tbody><tr><td><code>github.com/moxcomic/Archer/gamestate</code></td><td><code>github.com/moxcomic/Archer/internal/domain/gamestate</code></td></tr><tr><td><code>github.com/moxcomic/Archer/userinfo</code></td><td><code>github.com/moxcomic/Archer/internal/infra/userinfo</code></td></tr><tr><td><code>github.com/moxcomic/Archer/variable</code></td><td><code>github.com/moxcomic/Archer/internal/infra/variable</code></td></tr><tr><td><code>github.com/moxcomic/Archer/actions</code></td><td><code>github.com/moxcomic/Archer/internal/platform/actions</code></td></tr><tr><td><code>github.com/moxcomic/Archer/pusher</code></td><td><code>github.com/moxcomic/Archer/internal/notify/pusher</code></td></tr><tr><td><code>github.com/moxcomic/Archer/audio</code></td><td><code>github.com/moxcomic/Archer/internal/ui/audio</code></td></tr><tr><td><code>github.com/moxcomic/Archer/discord</code></td><td><code>github.com/moxcomic/Archer/internal/notify/discord</code></td></tr><tr><td><code>github.com/moxcomic/Archer/telegram</code></td><td><code>github.com/moxcomic/Archer/internal/notify/telegram</code></td></tr><tr><td><code>github.com/moxcomic/Archer/wechat</code></td><td><code>github.com/moxcomic/Archer/internal/notify/wechat</code></td></tr></tbody></table><blockquote><p><code>github.com/moxcomic/Archer/config</code> is <strong>unchanged</strong> (still a top-level public package).</p><p>⚠️ The <code>userinfo</code> method <code>IsVaild</code> was renamed to <code>IsValid</code> (typo fix). If your script called <code>.IsVaild()</code>, change it to <code>.IsValid()</code>; every other method name is unchanged.</p></blockquote><p>Call style is unchanged, for example:</p><div class="language-golang line-numbers-mode" data-highlighter="prismjs" data-ext="golang"><pre><code class="language-golang"><span class="line">// Only the import line above changes; the calls below stay exactly the same</span>
<span class="line">gamestate.Inst().GetHandTile()</span>
<span class="line">variable.Inst().GetInt(&quot;ju&quot;)</span>
<span class="line">actions.NewMajsoulActions()</span>
<span class="line">pusher.Inst().SendMasterText(&quot;...&quot;)</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="b-call-style-also-changed-split-merge" tabindex="-1"><a class="header-anchor" href="#b-call-style-also-changed-split-merge"><span>B. Call style also changed (split / merge)</span></a></h2><h3 id="b1-utils-split-into-4-packages-drop-inst" tabindex="-1"><a class="header-anchor" href="#b1-utils-split-into-4-packages-drop-inst"><span>B1. <code>utils</code> split into 4 packages (drop <code>.Inst()</code>)</span></a></h3><p>The old <code>utils.Inst().X(...)</code> now becomes the matching package&#39;s <strong>package-level function</strong> <code>pkg.X(...)</code>:</p><table><thead><tr><th>Old call</th><th>New import</th><th>New call</th></tr></thead><tbody><tr><td><code>utils.Inst().ChangeEngine(...)</code><br><code>utils.Inst().ChangeMainIndex(...)</code></td><td><code>github.com/moxcomic/Archer/internal/platformutil/engineswitch</code></td><td><code>engineswitch.ChangeEngine(...)</code><br><code>engineswitch.ChangeMainIndex(...)</code></td></tr><tr><td><code>utils.Inst().DispatchBot(...)</code><br><code>utils.Inst().DispatchBotGateway(...)</code></td><td><code>github.com/moxcomic/Archer/internal/platformutil/botdispatch</code></td><td><code>botdispatch.DispatchBot(...)</code><br><code>botdispatch.DispatchBotGateway(...)</code></td></tr><tr><td><code>utils.Inst().MouseMove(...)</code> / <code>MouseClick()</code> / <code>Screenshot(...)</code> / <code>SetMarker(...)</code> / <code>SetMarkerForPage(...)</code> / <code>SetMouseMarker(...)</code></td><td><code>github.com/moxcomic/Archer/internal/platformutil/pagecontrol</code></td><td><code>pagecontrol.MouseMove(...)</code> etc. (drop <code>Inst().</code>)</td></tr><tr><td><code>utils.Inst().Polarity(...)</code></td><td><code>github.com/moxcomic/Archer/internal/platformutil/extension</code></td><td><code>extension.Polarity(...)</code></td></tr></tbody></table><h3 id="b2-b3-countdown-countdown-and-player-info-playerinfo-—-removed" tabindex="-1"><a class="header-anchor" href="#b2-b3-countdown-countdown-and-player-info-playerinfo-—-removed"><span>B2 / B3. Countdown (<code>countdown</code>) and player info (<code>playerinfo</code>) — removed</span></a></h3><blockquote><p>⚠️ These two UI-state query packages were removed in the Majsoul Unity migration (see the notice at the top) and are no longer provided. The former <code>winmgr</code> / <code>huleshowmgr</code> / <code>scorechangemgr</code> / <code>playerinfomgr</code> / <code>otherplayerinfomgr</code> are all gone. Derive the corresponding state from the in-game protobuf event stream instead.</p></blockquote><h2 id="full-example-a-tenhou-room-script-before-and-after" tabindex="-1"><a class="header-anchor" href="#full-example-a-tenhou-room-script-before-and-after"><span>Full example: a Tenhou room script before and after</span></a></h2><p>Before (old paths + old <code>utils</code> call):</p><div class="language-golang line-numbers-mode" data-highlighter="prismjs" data-ext="golang"><pre><code class="language-golang"><span class="line">import (</span>
<span class="line">	&quot;github.com/moxcomic/Archer/utils&quot;</span>
<span class="line">	&quot;github.com/moxcomic/Archer/gamestate&quot;</span>
<span class="line">	&quot;github.com/moxcomic/Archer/userinfo&quot;</span>
<span class="line">	&quot;github.com/moxcomic/Archer/variable&quot;</span>
<span class="line">	tenhouclient &quot;github.com/moxcomic/engine/tenhou_client&quot;</span>
<span class="line">	&quot;github.com/moxcomic/lq&quot;</span>
<span class="line">)</span>
<span class="line"></span>
<span class="line">func dispatch() error {</span>
<span class="line">	return utils.Inst().DispatchBot(1000, 20, strconv.Itoa(ROOM_ID), &quot;high&quot;, &quot;4.1b&quot;, &quot;4.1b&quot;, &quot;4.1b&quot;)</span>
<span class="line">}</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>After:</p><div class="language-golang line-numbers-mode" data-highlighter="prismjs" data-ext="golang"><pre><code class="language-golang"><span class="line">import (</span>
<span class="line">	&quot;github.com/moxcomic/Archer/internal/platformutil/botdispatch&quot;</span>
<span class="line">	&quot;github.com/moxcomic/Archer/internal/domain/gamestate&quot;</span>
<span class="line">	&quot;github.com/moxcomic/Archer/internal/infra/userinfo&quot;</span>
<span class="line">	&quot;github.com/moxcomic/Archer/internal/infra/variable&quot;</span>
<span class="line">	tenhouclient &quot;github.com/moxcomic/engine/tenhou_client&quot;</span>
<span class="line">	&quot;github.com/moxcomic/lq&quot;</span>
<span class="line">)</span>
<span class="line"></span>
<span class="line">func dispatch() error {</span>
<span class="line">	return botdispatch.DispatchBot(1000, 20, strconv.Itoa(ROOM_ID), &quot;high&quot;, &quot;4.1b&quot;, &quot;4.1b&quot;, &quot;4.1b&quot;)</span>
<span class="line">}</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The <code>gamestate.Inst()</code> / <code>userinfo.Inst()</code> / <code>variable.Inst()</code> / <code>tenhouclient.Inst()</code> calls are untouched — only the import lines and the single <code>utils.Inst().DispatchBot</code> call changed.</p><h2 id="migration-checklist" tabindex="-1"><a class="header-anchor" href="#migration-checklist"><span>Migration checklist</span></a></h2><ol><li>Replace every <code>github.com/moxcomic/Archer/&lt;X&gt;</code> import in your script with the <code>internal/...</code> path from <strong>table A</strong>.</li><li><code>utils.Inst().X()</code> → per <strong>B1</strong>, change to the package-level function in <code>engineswitch</code> / <code>botdispatch</code> / <code>pagecontrol</code> / <code>extension</code> (drop <code>Inst().</code>).</li><li><code>winmgr</code> / <code>huleshowmgr</code> / <code>scorechangemgr</code> / <code>playerinfomgr</code> / <code>otherplayerinfomgr</code> and other UI-state query packages → <strong>removed in the Unity migration</strong>; delete those calls and derive state from the in-game event stream instead.</li><li><code>config</code>, <code>github.com/moxcomic/lq</code>, and <code>github.com/moxcomic/engine/tenhou_client</code> stay <strong>unchanged</strong>.</li><li>If you used <code>userinfo.Inst().IsVaild()</code>, change it to <code>IsValid()</code>.</li><li>Redeploy to the <code>/auto</code> push side. The local sample <code>scripts/auto.majsoul</code> has been updated and can be used as a reference.</li></ol><blockquote><p>The script actually loaded locally, <code>scripts/auto.go</code>, was updated alongside the refactor and passes the yaegi tests. The scripts you push via <code>/auto</code> are yours to update per this guide.</p></blockquote>`,27)])])}const r=o(n,[["render",i]]),l=JSON.parse('{"path":"/en/auto/migration.html","title":"Script Package Path Migration (internal refactor)","lang":"en-US","frontmatter":{"title":"Script Package Path Migration (internal refactor)"},"git":{"updatedTime":1780159975000,"contributors":[{"name":"moxcomic","username":"moxcomic","email":"37604141+moxcomic@users.noreply.github.com","commits":1,"url":"https://github.com/moxcomic"}],"changelog":[{"hash":"bd85283be4efc8b7a92196857431396f63beb744","time":1780159975000,"email":"37604141+moxcomic@users.noreply.github.com","author":"moxcomic","message":"docs(archer): 脚本 import 路径迁移至 internal/ + 新增脚本迁移指南(中英)"}]},"filePathRelative":"en/auto/migration.md"}');export{r as comp,l as data};
