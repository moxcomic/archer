import{_ as o,c,a as t,o as d}from"./app-BXqkdwf9.js";const i={};function n(r,e){return d(),c("div",null,[...e[0]||(e[0]=[t(`<h1 id="脚本包路径迁移指南" tabindex="-1"><a class="header-anchor" href="#脚本包路径迁移指南"><span>脚本包路径迁移指南</span></a></h1><blockquote><p><strong>适用对象</strong>：通过 <code>/auto</code> 接口下发<strong>远端 yaegi 脚本</strong>的开发者，以及维护本地 <code>scripts/*.majsoul</code> 样本的人。</p><p>本次重构把脚本可见的 Go 包搬进了 <code>internal/</code> 分层目录。<strong>你脚本里的 import 路径需要更新</strong>，否则 yaegi 在加载/运行时会报 <code>unable to find source</code> / <code>undefined symbol</code>。</p></blockquote><h2 id="一句话总结" tabindex="-1"><a class="header-anchor" href="#一句话总结"><span>一句话总结</span></a></h2><ul><li><strong>绝大多数包：只改 import 路径</strong>，<code>包名.Inst().方法()</code> 的调用写法<strong>完全不变</strong>。</li><li><strong>只有两类例外改了调用写法</strong>：① <code>utils</code> 被拆成 4 个包（去掉 <code>.Inst()</code>，改用包级函数）；② 倒计时三件套、玩家信息双胞胎分别合并成 <code>countdown</code> / <code>playerinfo</code>。</li><li><strong>方法名一律不变</strong>。<code>config</code> 路径不变。引擎包 <code>github.com/moxcomic/lq</code>、<code>github.com/moxcomic/engine/tenhou_client</code> 不受影响。</li></ul><h2 id="为什么会变" tabindex="-1"><a class="header-anchor" href="#为什么会变"><span>为什么会变</span></a></h2><p>yaegi 解释器靠一张 symbols 符号表把脚本里的 import 路径映射到真实的 Go 包。重构后这些包的物理位置（import 路径）变了，symbols 表已随之同步更新；但<strong>你脚本里写死的 import 路径需要你手动改</strong>——路径对不上，yaegi 找不到对应符号，就会在加载或运行时报错。</p><h2 id="a-只改-import-路径-调用写法不变" tabindex="-1"><a class="header-anchor" href="#a-只改-import-路径-调用写法不变"><span>A. 只改 import 路径（调用写法不变）</span></a></h2><table><thead><tr><th>旧 import</th><th>新 import</th></tr></thead><tbody><tr><td><code>github.com/moxcomic/Archer/gamestate</code></td><td><code>github.com/moxcomic/Archer/internal/domain/gamestate</code></td></tr><tr><td><code>github.com/moxcomic/Archer/userinfo</code></td><td><code>github.com/moxcomic/Archer/internal/infra/userinfo</code></td></tr><tr><td><code>github.com/moxcomic/Archer/variable</code></td><td><code>github.com/moxcomic/Archer/internal/infra/variable</code></td></tr><tr><td><code>github.com/moxcomic/Archer/actions</code></td><td><code>github.com/moxcomic/Archer/internal/platform/actions</code></td></tr><tr><td><code>github.com/moxcomic/Archer/pusher</code></td><td><code>github.com/moxcomic/Archer/internal/notify/pusher</code></td></tr><tr><td><code>github.com/moxcomic/Archer/audio</code></td><td><code>github.com/moxcomic/Archer/internal/ui/audio</code></td></tr><tr><td><code>github.com/moxcomic/Archer/discord</code></td><td><code>github.com/moxcomic/Archer/internal/notify/discord</code></td></tr><tr><td><code>github.com/moxcomic/Archer/telegram</code></td><td><code>github.com/moxcomic/Archer/internal/notify/telegram</code></td></tr><tr><td><code>github.com/moxcomic/Archer/wechat</code></td><td><code>github.com/moxcomic/Archer/internal/notify/wechat</code></td></tr><tr><td><code>github.com/moxcomic/Archer/mgr/lobbymgr</code></td><td><code>github.com/moxcomic/Archer/internal/ui/widget/lobbymgr</code></td></tr><tr><td><code>github.com/moxcomic/Archer/mgr/desktop_mgr</code></td><td><code>github.com/moxcomic/Archer/internal/ui/widget/desktopmgr</code></td></tr><tr><td><code>github.com/moxcomic/Archer/mgr/infomgr</code></td><td><code>github.com/moxcomic/Archer/internal/ui/widget/infomgr</code></td></tr><tr><td><code>github.com/moxcomic/Archer/mgr/gameend_mgr</code></td><td><code>github.com/moxcomic/Archer/internal/ui/widget/gameendmgr</code></td></tr><tr><td><code>github.com/moxcomic/Archer/mgr/activity_mgr</code></td><td><code>github.com/moxcomic/Archer/internal/ui/widget/activitymgr</code></td></tr><tr><td><code>github.com/moxcomic/Archer/mgr/activity_sign_mgr</code></td><td><code>github.com/moxcomic/Archer/internal/ui/widget/activitysignmgr</code></td></tr><tr><td><code>github.com/moxcomic/Archer/mgr/monthly_mgr</code></td><td><code>github.com/moxcomic/Archer/internal/ui/widget/monthlymgr</code></td></tr></tbody></table><blockquote><p><code>github.com/moxcomic/Archer/config</code> <strong>保持不变</strong>（仍是顶层公开包）。</p><p>⚠️ <code>userinfo</code> 的方法 <code>IsVaild</code> 已更名为 <code>IsValid</code>（修正拼写）。如果你的脚本调用过 <code>.IsVaild()</code>，请改为 <code>.IsValid()</code>；其余方法名一律不变。</p></blockquote><p>调用写法不变，举例：</p><div class="language-golang line-numbers-mode" data-highlighter="prismjs" data-ext="golang"><pre><code class="language-golang"><span class="line">// 只改了上面 import 的那一行，下面的调用一字不动</span>
<span class="line">gamestate.Inst().GetHandTile()</span>
<span class="line">variable.Inst().GetInt(&quot;ju&quot;)</span>
<span class="line">actions.NewMajsoulActions()</span>
<span class="line">pusher.Inst().SendMasterText(&quot;...&quot;)</span>
<span class="line">lobbymgr.Inst().Enable()</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="b-调用写法也变了-拆包-合并" tabindex="-1"><a class="header-anchor" href="#b-调用写法也变了-拆包-合并"><span>B. 调用写法也变了（拆包 / 合并）</span></a></h2><h3 id="b1-utils-拆成-4-个包-去掉-inst" tabindex="-1"><a class="header-anchor" href="#b1-utils-拆成-4-个包-去掉-inst"><span>B1. <code>utils</code> 拆成 4 个包（去掉 <code>.Inst()</code>）</span></a></h3><p>原来的 <code>utils.Inst().X(...)</code> 现在改成对应包的<strong>包级函数</strong> <code>pkg.X(...)</code>：</p><table><thead><tr><th>旧调用</th><th>新 import</th><th>新调用</th></tr></thead><tbody><tr><td><code>utils.Inst().ChangeEngine(...)</code><br><code>utils.Inst().ChangeMainIndex(...)</code></td><td><code>github.com/moxcomic/Archer/internal/platformutil/engineswitch</code></td><td><code>engineswitch.ChangeEngine(...)</code><br><code>engineswitch.ChangeMainIndex(...)</code></td></tr><tr><td><code>utils.Inst().DispatchBot(...)</code><br><code>utils.Inst().DispatchBotGateway(...)</code></td><td><code>github.com/moxcomic/Archer/internal/platformutil/botdispatch</code></td><td><code>botdispatch.DispatchBot(...)</code><br><code>botdispatch.DispatchBotGateway(...)</code></td></tr><tr><td><code>utils.Inst().MouseMove(...)</code> / <code>MouseClick()</code> / <code>Screenshot(...)</code> / <code>SetMarker(...)</code> / <code>SetMarkerForPage(...)</code> / <code>SetMouseMarker(...)</code></td><td><code>github.com/moxcomic/Archer/internal/platformutil/pagecontrol</code></td><td><code>pagecontrol.MouseMove(...)</code> 等（去掉 <code>Inst().</code>）</td></tr><tr><td><code>utils.Inst().Polarity(...)</code></td><td><code>github.com/moxcomic/Archer/internal/platformutil/extension</code></td><td><code>extension.Polarity(...)</code></td></tr></tbody></table><h3 id="b2-倒计时三件套合并为-countdown" tabindex="-1"><a class="header-anchor" href="#b2-倒计时三件套合并为-countdown"><span>B2. 倒计时三件套合并为 <code>countdown</code></span></a></h3><table><thead><tr><th>旧</th><th>新 import</th><th>新调用</th></tr></thead><tbody><tr><td><code>winmgr.Inst()</code>（<code>mgr/winmgr</code>）</td><td><code>github.com/moxcomic/Archer/internal/ui/widget/countdown</code></td><td><code>countdown.Win()</code></td></tr><tr><td><code>huleshowmgr.Inst()</code>（<code>mgr/huleshow_mgr</code>）</td><td>同上</td><td><code>countdown.Huleshow()</code></td></tr><tr><td><code>scorechangemgr.Inst()</code>（<code>mgr/scorechange_mgr</code>）</td><td>同上</td><td><code>countdown.Scorechange()</code></td></tr></tbody></table><p>方法名（<code>.Enable()</code> / <code>.Text()</code> / <code>.TimerCount()</code> / <code>.OnConfirm()</code> / <code>.BtnConfirmVisible()</code>）不变。例：<code>winmgr.Inst().TimerCount()</code> → <code>countdown.Win().TimerCount()</code>。</p><h3 id="b3-玩家信息双胞胎合并为-playerinfo" tabindex="-1"><a class="header-anchor" href="#b3-玩家信息双胞胎合并为-playerinfo"><span>B3. 玩家信息双胞胎合并为 <code>playerinfo</code></span></a></h3><table><thead><tr><th>旧</th><th>新 import</th><th>新调用</th></tr></thead><tbody><tr><td><code>playerinfomgr.Inst()</code>（<code>mgr/playerinfo_mgr</code>）</td><td><code>github.com/moxcomic/Archer/internal/ui/widget/playerinfo</code></td><td><code>playerinfo.Self()</code></td></tr><tr><td><code>otherplayerinfomgr.Inst()</code>（<code>mgr/otherplayerinfo_mgr</code>）</td><td>同上</td><td><code>playerinfo.Other()</code></td></tr></tbody></table><p>方法名（<code>.Enable()</code> / <code>.Show(id)</code> / <code>.Close()</code>）不变。</p><h2 id="完整示例-天凤个室脚本迁移前后" tabindex="-1"><a class="header-anchor" href="#完整示例-天凤个室脚本迁移前后"><span>完整示例：天凤个室脚本迁移前后</span></a></h2><p>迁移前（旧路径 + 旧 <code>utils</code> 调用）：</p><div class="language-golang line-numbers-mode" data-highlighter="prismjs" data-ext="golang"><pre><code class="language-golang"><span class="line">import (</span>
<span class="line">	&quot;github.com/moxcomic/Archer/utils&quot;</span>
<span class="line">	&quot;github.com/moxcomic/Archer/gamestate&quot;</span>
<span class="line">	&quot;github.com/moxcomic/Archer/userinfo&quot;</span>
<span class="line">	&quot;github.com/moxcomic/Archer/variable&quot;</span>
<span class="line">	tenhouclient &quot;github.com/moxcomic/engine/tenhou_client&quot;</span>
<span class="line">	&quot;github.com/moxcomic/lq&quot;</span>
<span class="line">)</span>
<span class="line"></span>
<span class="line">func dispatch() error {</span>
<span class="line">	return utils.Inst().DispatchBot(1000, 20, strconv.Itoa(ROOM_ID), &quot;high&quot;, &quot;4.1b&quot;, &quot;4.1b&quot;, &quot;4.1b&quot;)</span>
<span class="line">}</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>迁移后：</p><div class="language-golang line-numbers-mode" data-highlighter="prismjs" data-ext="golang"><pre><code class="language-golang"><span class="line">import (</span>
<span class="line">	&quot;github.com/moxcomic/Archer/internal/platformutil/botdispatch&quot;</span>
<span class="line">	&quot;github.com/moxcomic/Archer/internal/domain/gamestate&quot;</span>
<span class="line">	&quot;github.com/moxcomic/Archer/internal/infra/userinfo&quot;</span>
<span class="line">	&quot;github.com/moxcomic/Archer/internal/infra/variable&quot;</span>
<span class="line">	tenhouclient &quot;github.com/moxcomic/engine/tenhou_client&quot;</span>
<span class="line">	&quot;github.com/moxcomic/lq&quot;</span>
<span class="line">)</span>
<span class="line"></span>
<span class="line">func dispatch() error {</span>
<span class="line">	return botdispatch.DispatchBot(1000, 20, strconv.Itoa(ROOM_ID), &quot;high&quot;, &quot;4.1b&quot;, &quot;4.1b&quot;, &quot;4.1b&quot;)</span>
<span class="line">}</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>gamestate.Inst()</code> / <code>userinfo.Inst()</code> / <code>variable.Inst()</code> / <code>tenhouclient.Inst()</code> 等调用一字未动——只有 import 行和 <code>utils.Inst().DispatchBot</code> 这一处调用变了。</p><h2 id="迁移检查清单" tabindex="-1"><a class="header-anchor" href="#迁移检查清单"><span>迁移检查清单</span></a></h2><ol><li>把脚本里所有 <code>github.com/moxcomic/Archer/&lt;X&gt;</code> 的 import 按 <strong>A 表</strong>替换为 <code>internal/...</code> 路径。</li><li><code>utils.Inst().X()</code> → 按 <strong>B1</strong> 改成 <code>engineswitch</code> / <code>botdispatch</code> / <code>pagecontrol</code> / <code>extension</code> 对应的包级函数（去掉 <code>Inst().</code>）。</li><li><code>winmgr</code> / <code>huleshowmgr</code> / <code>scorechangemgr</code> → 按 <strong>B2</strong> 改成 <code>countdown.Win()</code> / <code>Huleshow()</code> / <code>Scorechange()</code>。</li><li><code>playerinfomgr</code> / <code>otherplayerinfomgr</code> → 按 <strong>B3</strong> 改成 <code>playerinfo.Self()</code> / <code>Other()</code>。</li><li><code>config</code>、<code>github.com/moxcomic/lq</code>、<code>github.com/moxcomic/engine/tenhou_client</code> <strong>保持不变</strong>。</li><li>若用过 <code>userinfo.Inst().IsVaild()</code>，改为 <code>IsValid()</code>。</li><li>重新部署到 <code>/auto</code> 下发端。本地样本 <code>scripts/auto.majsoul</code> 已同步更新，可作参考。</li></ol><blockquote><p>本地实际加载的脚本 <code>scripts/auto.go</code> 已随重构同步更新并通过 yaegi 测试。远端 <code>/auto</code> 下发的脚本需要你按本指南更新。</p></blockquote>`,30)])])}const s=o(i,[["render",n]]),l=JSON.parse('{"path":"/zh/auto/migration.html","title":"脚本包路径迁移（internal 重构）","lang":"zh-CN","frontmatter":{"title":"脚本包路径迁移（internal 重构）"},"git":{"updatedTime":1780159975000,"contributors":[{"name":"moxcomic","username":"moxcomic","email":"37604141+moxcomic@users.noreply.github.com","commits":1,"url":"https://github.com/moxcomic"}],"changelog":[{"hash":"bd85283be4efc8b7a92196857431396f63beb744","time":1780159975000,"email":"37604141+moxcomic@users.noreply.github.com","author":"moxcomic","message":"docs(archer): 脚本 import 路径迁移至 internal/ + 新增脚本迁移指南(中英)"}]},"filePathRelative":"zh/auto/migration.md"}');export{s as comp,l as data};
